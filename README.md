## How to run the project

In fluidui/ and hack/ run:
```ps1
pnpm install
```

Then approve-builds for argon2, jsonwebtoken and stuff.

Then in fluidui and hack run
```
pnpm run dev
```

go to: `localhost:5173` to see the site.
